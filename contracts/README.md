# Shared Contracts

This directory is reserved for executable contracts shared across CodeTrace components.

Contract definitions are intentionally deferred during repository initialization. No schema, event, feature, classifier-output, or other executable contract should be added until its contents and ownership have been explicitly agreed.

Future contracts should be added here only when they are required by more than one component and have an approved definition.