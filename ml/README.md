# Offline ML

This directory is reserved for offline machine-learning work such as approved data preparation, experimentation, training, and evaluation.

Live API behavior and component runtime integration do not belong here. During repository initialization, this directory contains no generators, training scripts, model implementations, datasets, or model artifacts.